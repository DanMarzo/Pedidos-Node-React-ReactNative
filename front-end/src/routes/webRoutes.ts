const authWebRoutes = {
    auth: "/auth",
    login: "logIn",
    signUp: "signUp"
}
const homeWebRoutes = {
    home: "/home",
}
const categoryWebRoutes = {
    categorias: "/categorias"
}
export{authWebRoutes,homeWebRoutes,categoryWebRoutes}